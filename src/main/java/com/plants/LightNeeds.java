package com.plants;
// create an Enum to represent the light requirement categories a plant can have.
// Used to restrict lightNeeds to a fixed set of valid values like a constant variable instead of allowing any String for lightNeeds

public enum LightNeeds {

    LOW,
    MEDIUM,
    BRIGHT
}
